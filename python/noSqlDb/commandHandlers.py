
def handlePut(key, value):
    """Inserting single value into key
    and return tuple containig True and the message back to client"""
    pass
    
def handlGet(key):
    """Returns tuple containig True and value, if value exists
    or false to the client"""
    pass

def handleDelete(key):
    """Returns tuple containig true if value exists and 
    deleted or false along with the message"""
    pass

def handleGetList(key):
    """Returns tuple containig True and list of elements if key exists 
    along with the message"""
    pass

def handlePutList(key, value):
    """Returns tuple containing True if list is inserted succesfully to the 
    key and also returns the message"""
    pass 
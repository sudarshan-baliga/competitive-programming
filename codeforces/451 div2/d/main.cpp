#include <bits/stdc++.h>

using namespace std;

int main()
{
    long long n,m,k,a[1000000],temp = 0;
    cin>>n>>m>>k;
    for(int i = 0; i <  n ; i++)
        cin>>a[i];
    sort(a,a+n);

    for(int i = 0 ; i < n; i++)
    {
        if(a[i] == temp)
            temp++;
        else
            temp+ =

    }
}
